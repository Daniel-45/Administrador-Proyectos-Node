doctype html
html(lang="en")
    head
        include partials/header
        
    body.login
        .contenedor-formulario
            h1= titulo
            p.texto-restablecer Añade tu e-mail para restablecer la contraseña

            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            div 
                                p.alerta(class=`${categoria}`)= error

            form.caja-login(method="POST" action="/restablecer-password")
                .campo 
                    label(for="email") E-mail
                    input(type="email" name="email" placeholder="name@example.com" value= email)

                .campo.enviar 
                    input.boton(type="submit" value="Restablecer Contraseña")

                .campo.acciones 
                    a(href="/iniciar-sesion") Volver a iniciar sesión