mixin formularioProyecto(proyecto = {})

    form.agregar-proyecto(action=`/nuevo-proyecto/${proyecto.id || ''} ` method="POST")  
        .campo 
            label(for="nombre") Proyecto 
            input(type="text" value=proyecto.nombre id="nombre" name="nombre" placeholder="Título del proyecto")
        .campo.enviar 
            input(type="submit" value=`${proyecto.nombre ? 'Guardar' : 'Crear'} ` class="boton")